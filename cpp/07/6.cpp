#include <iostream> // cout, endl, cin
#include <vector> 
#include <cstdlib>
#include <cstring>
using namespace std;

#define rep(i,n) for (int i = 0; i < (int)(n); ++i)

int main() {
  int H, W;
  cin >> H >> W;

  char str_len[H][W];

  rep(i, H) {
    cin >> str_len[i];
  }

  rep(i, H){

  }
}
